{
  "name": "backend",
  "version": "1.0.0",
  "description": "Videoselz take-home: shoppable video analytics dashboard",
  "license": "ISC",
  "author": "Hirak Mahata",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "seed": "prisma generate && prisma migrate deploy && node prisma/seed.js",
    "reset-db": "prisma generate && prisma migrate deploy && node prisma/seed.js --reset"
  },
  "dependencies": {
    "@prisma/adapter-better-sqlite3": "^7.9.1",
    "@prisma/client": "^7.9.1",
    "better-sqlite3": "^13.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "prisma": "^7.9.1"
  }
}
